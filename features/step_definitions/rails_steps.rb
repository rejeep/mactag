Given /^rails lives in vendor$/ do
  pending
end

Given /^a rails mactag config with the following tags$/ do |table|
  pending
end

Given /^rails version "([^\"]*)" lives in vendor$/ do |arg1|
  pending
end

Given /^a rails mactag config file with the following tags$/ do |table|
  pending
end
